# local: 인프라 의존을 최소화하는 local 개발 환경. w/o vault(without-local-dev.properties)
application:
  kakao-api:
    #   admin-key:   from local.properties
    #   domain-id:   from local.properties
    openlink-url: http://kapi.kakao.com/v1/internal/talk/openchat
  bgms:
    api-url: https://www.biztalk-api.com
    id: groundx_prod_dev
    password: ${bgms_pw}  # from local.properties
  developer:
    super-admin: gene.goh@groundx.xyz
    emails:
      - jordan.jung@groundx.xyz
      - ian.han@groundx.xyz
      - winnie.byun@groundx.xyz
      - rachel.lee@groundx.xyz
      - ted.jeong@groundx.xyz
      - twinsen.kim@groundx.xyz
      - sello.lee@groundx.xyz

logging:
  level:
    sql: debug
    openfeign: debug
    org.flywaydb.core.internal.sqlscript.DefaultSqlScriptExecutor: debug

management:
  newrelic:
    metrics:
      export:
        enabled: false
  endpoints:
    web:
      exposure:
        include: '*'

cloud:
  aws:
    s3:
      bucket: klip-membership-fe-dev
      prefix: dist
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false
    cloudfront:
      distribution-domain: membership.dev.klipwallet.com

spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            logger-level: full
    vault:
      enabled: false
  config:
    import:
      - optional:classpath:/local.properties   # Generate it yourself
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create
  security:
    oauth2:
      client:
        registration:
          google:
            provider: google
            client-id: 245508089677-ne4im3tniucl2sqar65kefn3fakecqdp.apps.googleusercontent.com
            client-secret: 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
            scope: profile, email

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    oauth:
      app-name: ${spring.security.oauth2.client.registration.google.provider}
      client-id: ${spring.security.oauth2.client.registration.google.client-id}
      client-secret: ${spring.security.oauth2.client.registration.google.client-secret}
      realm: ${spring.security.oauth2.client.registration.google.provider}


